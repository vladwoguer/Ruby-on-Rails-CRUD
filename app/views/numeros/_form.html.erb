


<%= form_with(model: numero, local: true) do |form| %>
  <% if numero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(numero.errors.count, "error") %> prohibited this numero from being saved:</h2>

      <ul>
      <% numero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numero %>
    <%= form.text_field :numero, id: "ex1", 'data-slider-id': 'ex1Slider', type: "text", 'data-slider-min': "0", 'data-slider-max': "20", 
       'data-slider-step': "1", 'data-slider-value': "14" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
