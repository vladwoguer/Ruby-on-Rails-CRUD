<%= form_with(model: testis, local: true) do |form| %>
  <% if testis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testis.errors.count, "error") %> prohibited this testis from being saved:</h2>

      <ul>
      <% testis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :testis_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
